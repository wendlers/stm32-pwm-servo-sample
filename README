This is the example firmware for my article posted on http://gpio.kaltpost.de/?page_id=412.

The tutorial describes how to drive two servos (e.g. for a pan-tilt unit) through a PWM signal on an ARM Cortex-M3.

To build the example you need an arm cross compiler and libopencm3 installed.
